name: Testing in CI
on:
  push:
    branches:
      - test
jobs:
  test:
    runs-on: ubuntu-latest
#    services:
    steps:
      - name: Get Code
        uses: actions/checkout@v3
      - name: Get Commit ID
        run: git log --pretty=oneline
#      - name: Get Changes
#        run: git diff
